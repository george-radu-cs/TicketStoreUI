import { RedirectErrorDirective } from './redirect-error.directive';

describe('RedirectErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new RedirectErrorDirective();
    expect(directive).toBeTruthy();
  });
});
