import { ButtonRedirectDirective } from './button-redirect.directive';

describe('ButtonRedirectDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonRedirectDirective();
    expect(directive).toBeTruthy();
  });
});
